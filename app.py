import streamlit as st

st.set_page_config(page_title="Streamlit on AWS App Runner", layout="centered")

st.title("🚀 Hello from Streamlit on App Runner!")
st.write("This app is running on AWS App Runner via a Docker container.")
st.success("Deployment was successful!")
